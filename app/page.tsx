export default function Home() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-16">
      <h1 className="text-2xl font-bold text-ink">OBLUE</h1>
      <p className="mt-2 text-text-2">
        Nền tảng lớp học cho giáo viên ngoại ngữ. Khung dự án vừa dựng xong — chưa có màn nào.
      </p>
      <p className="mt-6 text-text-3">
        Bản mẫu giao diện đã duyệt nằm ở <code className="rounded-s bg-field px-1.5 py-0.5 text-text">design/_reference/</code>.
        Thứ tự dựng ghi trong <code className="rounded-s bg-field px-1.5 py-0.5 text-text">docs/PLAN.md</code>.
      </p>
    </main>
  )
}
